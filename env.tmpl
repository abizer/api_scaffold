PROJECT_NAME="exampleproject-api"

# these project names actually come from the justfile env, not above
DATABASE_USER="op://$PROJECT_NAME/DATABASE_USER/$APP_ENV"
DATABASE_PASSWORD="op://$PROJECT_NAME/DATABASE_PASSWORD/$APP_ENV"
DATABASE_HOST="op://$PROJECT_NAME/DATABASE_HOST/$APP_ENV"
DATABASE_PORT="op://$PROJECT_NAME/DATABASE_PORT/$APP_ENV"
DATABASE_NAME="op://$PROJECT_NAME/DATABASE_NAME/$APP_ENV"

DATABASE_ENGINE="sqlite"

AUTH_JWT_SECRET="op://$PROJECT_NAME/AUTH_JWT_SECRET/credential"